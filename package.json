{
  "name": "tinder-clone",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "devStart": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "Boudriou Allaoua",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "concurrently": "^3.6.0",
    "config": "^3.0.1",
    "mongoose": "^6.0.14",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "dotenv": "^10.0.0"
  }
}
